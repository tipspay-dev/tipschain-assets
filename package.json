{
  "name": "tipschain-assets",
  "version": "1.0.0",
  "private": true,
  "description": "Production asset registry pipeline for the TipsChain EVM ecosystem.",
  "type": "commonjs",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "validate": "ts-node --project tsconfig.json scripts/validate-assets.ts",
    "build": "ts-node --project tsconfig.json scripts/generate-list.ts"
  },
  "dependencies": {
    "ethers": "^6.14.4",
    "sharp": "^0.34.2",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
