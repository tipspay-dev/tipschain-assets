Imported source logos are staged here for future token directory onboarding.

Only the native TipsChain asset is wired into `tokens/1925/` because no EIP-55 token
addresses or `info.json` metadata were provided for the other logos.
